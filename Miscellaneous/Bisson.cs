using UnityEngine;
using System.Collections;

public class Bisson : MonoBehaviour {
	/*
	public Text winText;			// Reference for text. 


	if(other.gameObject.CompareTag(/layer"))
	{
		winText.Text = "You Win!"; 
	}

	*/ 
}
